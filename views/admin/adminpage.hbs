<section>
    <div class="container" style="margin-top: 30px;">
        <form action="/admin/searchUser" method="post">
            <div class="input-group mb-4">
                <input id="searchVal" name="user_search" type="search" placeholder="Search here..." aria-describedby="button-addon5"
                    class="form-control">
                <div class="input-group-append">
                    <button id="button-addon5" type="submit" class="btn btn-primary"> <i class="fa fa-search">Search</i> </button> 
                </div>
            </div>
        </form>
        {{#if searchFailed}}
        <center class="text-danger bg-warning">No such entry</center>
        {{/if}}
        <table class="table table-bordered text-center" id="productsTable">
            <thead style="background-image: linear-gradient(black, rgb(82, 155, 39), black">
                <tr class="text-light">
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Email</th>
                    <th>Gender</th>
                    <th>Options</th>
                </tr>
            </thead>
            {{#each users}}
            <tbody style="background-color: rgb(180, 233, 173);">
                <tr>
                    <td>{{this.first_name}}</td>
                    <td>{{this.last_name}}</td>
                    <td>{{this.email}}</td>
                    <td>{{this.gender}}</td>
                    <td><a href="/admin/editUser/{{this._id}}"><span class="material-symbols-outlined">
                                edit
                            </span></a><a href="/admin/deleteUser/{{this._id}}"
                            onclick="return confirm('Are you want to delete {{this.first_name}} ?')"><span
                                class="material-symbols-outlined">
                                delete
                            </span></a></td>
                </tr>
            </tbody>
            {{/each}}
        </table>

    </div>
    <div class="container d-flex justify-content-end">
        <a class="btn btn-success" href="/admin/adduserFromHead">Add User</a>
    </div>
</section>
{{!--
<script>
    //var $ = require( 'jquery' );
    //var DataTable = require( 'datatables.net' )(window, $);
    //import DataTable from 'datatables.net-dt';
    $(function () {
        $('#productsTable').DataTable();
    });
</script> --}}
<script>
    let searchVal = document.getElementById('searchVal')
</script>